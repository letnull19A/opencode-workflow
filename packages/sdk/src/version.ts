/** Версия контракта платформа↔workflow. Меняется при брейкинге формата
 * артефакта или протокола; артефакт с несовместимой версией отсекается
 * на загрузке (workflow.error), без проверок кода в рантайме. */
export const workflowFormat = 1;

export const sdkVersion = "1.0.0";